# 2026-03-05

- Worked with fei on evaluating `abhi1693/openclaw-mission-control` for deployment on Pi host with Docker.
- Decision: use Docker deployment on the Pi (OpenClaw host), access UI from MacBook browser.
- Next step: continue setup walkthrough and deployment execution.
- Reminder requested: continue tomorrow at 11:00 (Europe/Berlin).
- Switched focus from Mission Control to TrueRecall setup.
- Installed and enabled Qdrant as systemd service (`qdrant.service`) from upstream ARM64 binary on this Pi.
- Created Qdrant collection `memories_tr` with vector size 1024 (Cosine).
- Pulled Ollama embedding model `snowflake-arctic-embed2`.
- Installed and enabled watcher service (`mem-qdrant-watcher.service`) with OpenClaw sessions path `/home/masgi_bot/.openclaw/agents/main/sessions`.
- Patched watcher session selection logic to prefer active session files instead of hardcoding `agent:main:main`.
- Migrated TrueRecall watcher to support OpenAI embeddings via API (`EMBEDDING_PROVIDER=openai`, `text-embedding-3-small`).
- Recreated Qdrant `memories_tr` collection at 1536 dimensions for OpenAI small embeddings (old 1024-dim points removed).
- Added secure env file path for API key: `/home/masgi_bot/.config/true-recall.env` (expects `OPENAI_API_KEY=...`).
- Implemented semantic retrieval CLI in TrueRecall project: `scripts/search_q.py` + wrapper `scripts/search_q.sh` (OpenAI embeddings + Qdrant vector query).
- Added session backfill utility: `scripts/backfill_sessions.py` and backfilled 59 recent turns into `memories_tr`.
- Verified semantic search works with real results for Mission Control-related queries.
- Added `scripts/wrapper_mode.sh` as single-entry wrapper mode (message in → guarded recall context out).
